package Game;

private class ListListener implements ListSelectionListener
{
    public void valueChanged(ListSelectionEvent e)
    {
       
       
        ImageIcon icon = new ImageIcon(selection + ".jpg");//선택된 도시의 이미지 로딩
        imgLabel.setIcon(icon);//이미지 넣기
    }
}